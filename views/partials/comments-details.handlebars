{{#each this}}
  <div class="row">
    <div class="col s12 m5">
      <div class="card-panel teal">
        <h5 class="card-author white-text">Authored by:{{user.username}}, Date:{{created_at}}
        </h5>
        <span class="card-comment-text">{{text}}</span>
      </div>
    </div>
  </div>
{{/each}}